# Instance locale — jamais committé
.claude/settings.local.json

# Artefacts de pipeline (contenu ignoré, dossier conservé via son .gitignore interne)
# (règle portée par .pipeline/.gitignore)

# Dépendances d'outillage — jamais committées
node_modules/

# Traces d'outillage local (vérification visuelle, planificateur) — jamais committées
.playwright-mcp/
.claude/scheduled_tasks.lock

# Divers
Thumbs.db
.DS_Store
